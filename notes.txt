C:MoveToArea("MMAR01")
C:MoveToArea("MMAR01")
C:CreateItem("SW2H10")

C:CreateCreature("LAROTTLE")
C:CreateCreature("MMMARBLE")
C:CreateItem("SW2H10")
C:CreateItem("MMVERBELT")

C:GetGlobal("PillarMembersSlain","GLOBAL") while p
C:GetGlobal("LarotDefeated", "GLOBAL")

/////////////// Translation ang - optional
1. Angielskie sounds
- po prostu folder z sountrackami z inna nazwa i kopiowac do override
2. Translate dialogow
3. Translate .tra z Polish/

In the Baldur's Gate games, BALDUR.BCS (and BALDUR25.BCS in ToB) are also present. They have no specific object (and as such, local variables do not work in them), but run all the time.
Finally, PLAYER1D.BCS runs whenever the party rests, in a similar way to the NPC entries in PDIALOG.2DA. 